---
required_variables:
  - test_name
defaults:
  display_pattern: smoke
  intensity_threshold: 0
metadata:
  purpose: "Smoke test — minimal 'device alive' check that the fixture wiring works. Composes from the kit: setup_preamble + pattern_capture + assert_intensity."
---
@<primitives/setup_preamble.j2>
@<primitives/pattern_capture.j2>
@<primitives/assert_intensity.j2>
