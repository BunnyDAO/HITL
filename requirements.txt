# Runtime + dev Python deps. sc-compose itself is a Rust CLI installed
# separately: `brew install randlee/tap/sc-compose` (see README.md).
numpy>=1.26,<3
pytest>=8,<9
pyyaml>=6,<7   # used by tests/test_skill_doc.py to parse template frontmatter
